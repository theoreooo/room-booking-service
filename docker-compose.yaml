services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    environment:
      HTTP_ADDR: ${HTTP_ADDR:-0.0.0.0:8080}
      HTTP_READ_TIMEOUT: ${HTTP_READ_TIMEOUT:-5s}
      HTTP_WRITE_TIMEOUT: ${HTTP_WRITE_TIMEOUT:-10s}
      HTTP_IDLE_TIMEOUT: ${HTTP_IDLE_TIMEOUT:-60s}
      HTTP_SHUTDOWN_TIMEOUT: ${HTTP_SHUTDOWN_TIMEOUT:-30s}

      DB_HOST: postgres
      DB_PORT: ${DB_PORT:-5432}
      DB_USER: ${DB_USER:-booking}
      DB_PASSWORD: ${DB_PASSWORD:-booking}
      DB_NAME: ${DB_NAME:-booking}
      DB_SSLMODE: disable
      DB_CONNECT_TIMEOUT: ${DB_CONNECT_TIMEOUT:-10s}
      DB_MAX_OPEN_CONNS: ${DB_MAX_OPEN_CONNS:-25}
      DB_MAX_IDLE_CONNS: ${DB_MAX_IDLE_CONNS:-5}

      JWT_SECRET: ${JWT_SECRET:-secret}
      JWT_ACCESS_TOKEN_TTL: ${JWT_ACCESS_TOKEN_TTL:-24h}

      DUMMY_ADMIN_UUID: ${DUMMY_ADMIN_UUID:-00000000-0000-0000-0000-000000000001}
      DUMMY_USER_UUID: ${DUMMY_USER_UUID:-00000000-0000-0000-0000-000000000002}

      CONFERENCE_BASE_URL: ${CONFERENCE_BASE_URL:-https://conference.mock.local}
      CONFERENCE_MOCK_FAIL_CREATE: ${CONFERENCE_MOCK_FAIL_CREATE:-false}
      CONFERENCE_MOCK_FAIL_DELETE: ${CONFERENCE_MOCK_FAIL_DELETE:-false}

      WORKER_SLOT_GENERATION_DAYS: ${WORKER_SLOT_GENERATION_DAYS:-7}
      WORKER_INTERVAL: ${WORKER_INTERVAL:-1h}

      LOG_LEVEL: ${LOG_LEVEL:-debug}
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ${DB_USER:-booking}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-booking}
      POSTGRES_DB: ${DB_NAME:-booking}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-booking} -d ${DB_NAME:-booking}"]
      interval: 5s
      timeout: 5s
      retries: 5

  migrate:
    image: migrate/migrate:v4.19.1
    volumes:
      - ./migrations:/migrations
    depends_on:
      postgres:
        condition: service_healthy
    command: 
      - -path=/migrations/
      - -database=postgres://${DB_USER:-booking}:${DB_PASSWORD:-booking}@postgres:5432/${DB_NAME:-booking}?sslmode=disable
      - up
    restart: "no"

  k6:
    image: grafana/k6:latest
    profiles: ["tools"]
    working_dir: /work
    volumes:
      - ./:/work
    environment:
      BASE_URL: http://app:8080
      REPORT_PATH: /work/loadtest/results.md
    depends_on:
      app:
        condition: service_started

volumes:
  postgres_data:
